FROM python:3.12-slim

WORKDIR /app

# Bad pattern: copying everything before dependency install invalidates cache
# whenever any source, data, or README file changes.
COPY . .

RUN pip install --no-cache-dir -r requirements.txt

ENV PYTHONPATH=/app/src

CMD ["python", "-m", "de_pipeline.main"]

