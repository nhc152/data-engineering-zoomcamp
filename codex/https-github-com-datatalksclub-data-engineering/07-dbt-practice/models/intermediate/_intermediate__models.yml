version: 2

models:
  - name: int_order_items__aggregated_to_order
    description: Order item measures aggregated to order grain.
    columns:
      - name: order_id
        tests: [not_null, unique]

  - name: int_payments__aggregated_to_order
    description: Payment measures aggregated to order grain.
    columns:
      - name: order_id
        tests: [not_null, unique]

