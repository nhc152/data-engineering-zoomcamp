# Example only. Do not put real secrets here.

APP_ENV=dev
DATABASE_URL=duckdb:///local_dev.duckdb
DBT_TARGET=dev

# Never commit real API tokens, passwords, or service account JSON.
EXAMPLE_API_TOKEN=replace_me_with_local_test_token_only

